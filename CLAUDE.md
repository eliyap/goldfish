# Goldfish

## Screenshots

When taking screenshots with rodney, save them to `/tmp/goldfish-screenshots/` instead of the repo directory. Pass the output path as the last argument to `rodney screenshot`, e.g.:

```
uvx rodney screenshot /tmp/goldfish-screenshots/name.png
```
